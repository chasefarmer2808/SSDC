import { RouteDataFilterPipe } from './route-data-filter.pipe';

describe('RouteDataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteDataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
