import { KeyDirective } from './key.directive';

describe('KeyDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyDirective();
    expect(directive).toBeTruthy();
  });
});
